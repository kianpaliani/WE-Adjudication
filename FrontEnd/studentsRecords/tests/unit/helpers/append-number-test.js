
import { appendNumber } from 'students-records/helpers/append-number';
import { module, test } from 'qunit';

module('Unit | Helper | append number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = appendNumber([42]);
  assert.ok(result);
});

